#include<iostream>
using namespace std;

// We will make function prototype (so that it will be ready to execute sum function later as we have written sum funtion below main )
int sum( int a ,int b);  // Accepted
// int sum( int a , b); NOT ACCEPTED
//int sum( int , int);  Accepted

void g(void);// accepted
// void g(); accepted

int main(){
    int num1 , num2;

    cout<<"Enter first number"<<endl;
    cin>>num1;
    cout<<"Enter Second number"<<endl;
    cin>>num2;
    cout<<"The sum is "<<sum(num1 , num2)<<endl;
    g();
     

return 0;
}

// we have made a sum function Here
int sum(int a, int b){

    int c = (a+b);
    return c;
}

void g(){

    cout<<"Hello, Good Morninig"<<endl;
}
